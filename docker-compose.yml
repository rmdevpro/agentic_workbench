services:
  blueprint:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: blueprint
    ports:
      - "7860:7860"
    volumes:
      - /path/to/your/data:/data
      # Optional: mount shared storage for the file browser
      # - /path/to/shared/storage:/mnt/storage
    restart: unless-stopped
