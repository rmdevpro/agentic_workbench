I will now read all the source files to identify every testable capability, including API endpoints, MCP tools, UI features, and more.

Based on a comprehensive audit of the Blueprint codebase, here is the complete list of testable capabilities:

### API Endpoints
1.  **Add Project** (API): POST `/api/projects` - Adds a local project path or clones a git repository into the workspace.
2.  **Auth Status** (API): GET `/api/auth/status` - Checks the validity and expiration of Claude CLI OAuth credentials.
3.  **Auth Login Trigger** (API): POST `/api/auth/login` - Spawns a non-interactive Claude CLI process to verify or initiate auth.
4.  **Keepalive Status** (API): GET `/api/keepalive/status` - Returns the current state, mode, and token expiration of the keepalive service.
5.  **Keepalive Mode Update** (API): PUT `/api/keepalive/mode` - Switches keepalive between `always`, `browser`, or `idle` modes.
6.  **State Sync** (API): GET `/api/state` - Synchronizes filesystem session data with the DB and returns a full project/session tree.
7.  **Create Session** (API): POST `/api/sessions` - Scaffolds a new Claude CLI session inside a background tmux instance.
8.  **Resume Session** (API): POST `/api/sessions/:sessionId/resume` - Attaches or re-initializes a tmux session for an existing session ID.
9.  **Rename Session** (API): PUT `/api/sessions/:sessionId/name` - Updates a session's display name in the database.
10. **Delete Session** (API): DELETE `/api/sessions/:sessionId` - Terminates tmux, deletes the JSONL history file, and removes the DB record.
11. **Get Session Config** (API): GET `/api/sessions/:sessionId/config` - Retrieves state, model overrides, and private notes for a specific session.
12. **Update Session Config** (API): PUT `/api/sessions/:sessionId/config` - Updates all metadata fields for a session in a single call.
13. **Archive Session** (API): PUT `/api/sessions/:sessionId/archive` - Legacy endpoint to toggle a session's archived state.
14. **Get Project Notes** (API): GET `/api/projects/:name/notes` - Retrieves shared markdown notes for a project.
15. **Update Project Notes** (API): PUT `/api/projects/:name/notes` - Persists shared markdown notes for a project.
16. **Get Session Notes** (API): GET `/api/sessions/:id/notes` - Retrieves private markdown notes for a session.
17. **Update Session Notes** (API): PUT `/api/sessions/:id/notes` - Persists private markdown notes for a session.
18. **List Tasks** (API): GET `/api/projects/:name/tasks` - Returns all todo/done tasks for a specific project.
19. **Add Task** (API): POST `/api/projects/:name/tasks` - Creates a new task with "human" or "agent" attribution.
20. **Complete Task** (API): PUT `/api/tasks/:id/complete` - Marks a task as done and records completion time.
21. **Reopen Task** (API): PUT `/api/tasks/:id/reopen` - Reverts a task to "todo" status.
22. **Delete Task** (API): DELETE `/api/tasks/:id` - Permanently removes a task from the database.
23. **List Messages** (API): GET `/api/projects/:name/messages` - Retrieves the 50 most recent inter-session messages for a project.
24. **Send Message** (API): POST `/api/projects/:name/messages` - Sends a message to another session via a filesystem bridge.
25. **Get Settings** (API): GET `/api/settings` - Retrieves all global application settings with defaults applied.
26. **Update Setting** (API): PUT `/api/settings` - Persists a key-value setting (e.g., model, keepalive mode).
27. **Read Global CLAUDE.md** (API): GET `/api/claude-md/global` - Reads the global instruction file from `~/.claude/CLAUDE.md`.
28. **Write Global CLAUDE.md** (API): PUT `/api/claude-md/global` - Writes the global instruction file to `~/.claude/CLAUDE.md`.
29. **Read Project CLAUDE.md** (API): GET `/api/projects/:name/claude-md` - Reads project-specific instructions from the project root.
30. **Write Project CLAUDE.md** (API): PUT `/api/projects/:name/claude-md` - Writes project-specific instructions to the project root.
31. **List MCP Servers** (API): GET `/api/mcp-servers` - Returns configured MCP servers from the Claude CLI `settings.json`.
32. **Update MCP Servers** (API): PUT `/api/mcp-servers` - Updates the Claude CLI `settings.json` with new MCP server configs.
33. **Search Conversations** (API): GET `/api/search` - Performs a keyword search across all session JSONL history files.
34. **Generate Summary** (API): POST `/api/sessions/:sessionId/summary` - Uses Claude to summarize the recent history of a session.
35. **Token Usage** (API): GET `/api/sessions/:sessionId/tokens` - Calculates context usage percentage from the last JSONL assistant entry.
36. **Smart Compaction** (API): POST `/api/sessions/:sessionId/smart-compact` - Triggers a multi-step plan update and `/compact` flow.
37. **Internal MCP Discovery** (API): GET `/api/mcp/tools` - Lists tools available to the internal MCP tool executor.
38. **Internal MCP Call** (API): POST `/api/mcp/call` - Executes an internal MCP tool by name with provided arguments.
39. **Model Listing** (API): GET `/v1/models` - OpenAI-compatible endpoint for listing available Claude models.
40. **Chat Completion** (API): POST `/v1/chat/completions` - OpenAI-compatible endpoint that routes prompts to Claude CLI sessions.
41. **List Webhooks** (API): GET `/api/webhooks` - Retrieves all registered outbound webhook endpoints.
42. **Add/Update Webhooks** (API): POST/PUT `/api/webhooks` - Configures external URLs to receive event notifications.
43. **External MCP Discovery** (API): GET `/api/mcp/external/tools` - Lists internal and admin tools for external system integration.
44. **External MCP Call** (API): POST `/api/mcp/external/call` - Executes admin tools (session creation, settings) for external consumers.
45. **Quorum Request** (API): POST `/api/quorum/ask` - Orchestrates a multi-model "junior" review synthesized by a "lead" model.

### MCP Tools
46. **search_sessions** (MCP): Search across session conversations for keywords with context snippets.
47. **summarize_session** (MCP): Generate an AI summary of discussion, accomplishments, and current state.
48. **list_sessions** (MCP): List all sessions for a project with metadata.
49. **get_project_notes** (MCP): Read the shared project markdown notes.
50. **get_session_notes** (MCP): Read session-specific private notes.
51. **get_tasks** (MCP): List all project tasks and their statuses.
52. **add_task** (MCP): Append a new task to the project task list.
53. **complete_task** (MCP): Mark a specific project task as done.
54. **get_project_claude_md** (MCP): Read the project's instruction file.
55. **read_plan** (MCP): Read the session's `.blueprint/plans/` markdown file.
56. **update_plan** (MCP): Write/Update the session's plan file for context recovery.
57. **smart_compaction** (MCP): Proactively trigger the automated plan-update-and-compact workflow.
58. **ask_quorum** (MCP): Initiate a multi-model quorum analysis on a complex technical question.
59. **send_message** (MCP): Send cross-session messages via uniquely-named bridge files.
60. **create_session** (MCP/Admin): Create a new CLI session for any project.
61. **delete_session** (MCP/Admin): Terminate and delete a specific session.
62. **set_session_state** (MCP/Admin): Change a session's visibility (active, archived, hidden).
63. **get_token_usage** (MCP/Admin): Monitor context token usage for any session.
64. **update_settings** (MCP/Admin): Remotely update Blueprint application settings.

### UI Features
65. **Project/Session Sidebar** (UI): Hierarchical tree view of all projects and sessions with status indicators.
66. **Session Filtering** (UI): Real-time toggle between active, archived, hidden, and all sessions.
67. **Session Sorting** (UI): Reorder sessions by date, name, or message frequency.
68. **Real-time Terminal** (UI): Interactive shell access to Claude CLI via xterm.js and PTY.
69. **Tabbed Navigation** (UI): Browser-like tabs for managing multiple concurrent CLI sessions.
70. **Notes/Tasks/CLAUDE.md Panel** (UI): Context-aware right panel for project metadata and instructions.
71. **Auto-saving Editors** (UI): Markdown editors for notes and instructions that save on input idle.
72. **Task Board** (UI): Interactive todo list with checkbox completion and deletion.
73. **Inter-session Message Log** (UI): Feed of messages exchanged between agents across sessions.
74. **Status Bar** (UI): Live dashboard showing model, token usage, and context percentage.
75. **Settings Management** (UI): Modal for configuring themes, fonts, models, and MCP servers.
76. **Session Config Modal** (UI): UI for editing session name, state, and model overrides.
77. **Auth Recovery Modal** (UI): UI for detecting OAuth login URLs and submitting authorization codes.
78. **Session Summary View** (UI): Modal for displaying AI-generated summaries and recent messages.

### WebSocket Behaviors
79. **PTY Stream** (WebSocket): Full-duplex binary stream between the web UI and background tmux sessions.
80. **Viewport Resize** (WebSocket): Dynamic terminal resizing that propagates to the underlying tmux PTY.
81. **Heartbeat** (WebSocket): Ping/Pong checks to prevent connection timeout.
82. **Auto-reconnect** (WebSocket): Exponential backoff logic to restore detached terminal sessions.

### Database Operations
83. **WAL Mode Optimization** (DB): Write-Ahead Logging for high-concurrency database access.
84. **Schema Migrations** (DB): Automated column addition for notes, states, and model overrides.
85. **Foreign Key Integrity** (DB): Cascading deletes and referential integrity between projects and sessions.
86. **JSON-in-SQLite** (DB): Storage of complex settings and webhook configurations as JSON strings.

### Configuration Options
87. **PORT** (Config): Environment variable to set the listener port.
88. **WORKSPACE** (Config): Environment variable to set the project root directory.
89. **default_model** (Config): Setting for the primary Claude model used for new sessions.
90. **keepalive_mode** (Config): Persistence strategy (`always`, `browser`, `idle`).
91. **keepalive_idle_minutes** (Config): Timeout duration for the idle keepalive mode.
92. **quorum_lead_model** (Config): Model used for final quorum synthesis.

### Error Paths & Safeguards
93. **Project Conflict Detection** (Error): 409 error when attempting to add an existing directory.
94. **Token Expiration Detection** (Error): Identifies expired credentials and triggers UI re-auth.
95. **Compaction Nudge System** (Error): Monitors token usage at 65%, 75%, 85%, and auto-compacts at 90%.
96. **Quorum Path Traversal Protection** (Error): Prevents junior agents from reading files outside the project CWD.
97. **PTY Attachment Failure** (Error): Graceful error handling for attempting to attach to non-existent tmux sessions.
98. **Webhook Timeout** (Error): 5-second timeout on outbound event notifications to prevent server hangs.
